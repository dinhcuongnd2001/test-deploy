import React from "react";
import "./SessionFood.scss";
function SessionFood({ title }) {
  return (
    <div className="sessionFood">
      <h2 className="title">{title}</h2>
    </div>
  );
}

export default SessionFood;
